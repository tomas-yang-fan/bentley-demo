@model webClient.Models.Shipping
@{
    ViewData["Title"] = "Shipping";
}

<h2>Welcome</h2>
    <input type="hidden" value="@Model"/>
    <table class="table">
        <thead>
            <tr>
                <th>
                    TrackingNumber
                </th>
                <th>
                    OrderNumber
                </th>
                <th>
                    ShipCharge
                </th>
                <th>
                    IsShipped
                </th>
                <th>
                    
                </th>
            </tr>
        </thead>
        <tbody>
            
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => @Model.TrackingNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => @Model.OrderNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => @Model.ShipCharge)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => @Model.IsShipped)
                    |

                </td>

                <td>
                    <a asp-action="CommingSoon">Tricking</a>

                </td>
                
            </tr>
        </tbody>
    </table>


    <div>
        <a asp-action="Orders">go to orders</a>
    </div>